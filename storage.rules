service firebase.storage {
  match /b/{bucket}/o {
    match /Jukebox/{userId}/{document=**} {
      allow read, create: if request.auth != null;
      allow delete: if request.auth.uid == userId;
    }
  }
}